import { AshaBackHeader } from '@/components/asha/AshaBackHeader'; import { DetailRow } from '@/components/asha/DetailRow'; import { EmptyState } from '@/components/asha/EmptyState'; import { SemanticStatusBadge } from '@/components/asha/SemanticStatusBadge'; import { AppButton } from '@/components/ui/AppButton'; import { AppText } from '@/components/ui/AppText'; import { BaseCard } from '@/components/ui/BaseCard'; import { Screen } from '@/components/ui/Screen'; import { getMockVaccinationsForPatients } from '@/data/mockVaccinations'; import { useAshaWorkspace } from '@/hooks/useAshaQueries'; import { ageFromDob, formatDate } from '@/utils/asha'; import { vaccinationStatusPresentation } from '@/utils/recordStatus'; import { router, useLocalSearchParams } from 'expo-router'; import { ActivityIndicator, View } from 'react-native';
export default function VaccinationDetailsScreen() { const { id } = useLocalSearchParams<{ id: string }>(); const { patients, isLoading, error } = useAshaWorkspace(); const vaccination = getMockVaccinationsForPatients(patients.map((patient) => patient.id)).find((item) => item.id === Number(id)); const patient = patients.find((item) => item.id === vaccination?.patientId); if (isLoading) return <Screen header={<AshaBackHeader title="Vaccination Details" />}><View className="flex-1 items-center justify-center"><ActivityIndicator color="#0F766E" /><AppText variant="caption" className="mt-3">Loading patient details...</AppText></View></Screen>; if (error || !vaccination || !patient) return <Screen header={<AshaBackHeader title="Vaccination Details" />}><EmptyState title="Vaccination data unavailable" message="No matching vaccination record is available for this patient." /></Screen>; const status = vaccinationStatusPresentation[vaccination.status]; return <Screen scrollable header={<AshaBackHeader title="Vaccination Details" />}><BaseCard><AppText variant="subtitle" className="text-primary">Patient</AppText><AppText variant="title" className="mt-1">{patient.name}</AppText><AppText variant="caption">{ageFromDob(patient.dateOfBirth)} years - {patient.gender}</AppText><AppButton title="View Patient" variant="outline" className="mt-4" onPress={() => router.push({ pathname: '/(asha)/patients/[id]', params: { id: String(patient.id) } })} /></BaseCard><AppText variant="title" className="mb-3 mt-2">Vaccination Information</AppText><BaseCard><DetailRow label="Vaccine" value={vaccination.name} /><DetailRow label="Dose" value={vaccination.dose} /><View className="mb-3"><AppText variant="caption">Status</AppText><SemanticStatusBadge label={status.label} tone={status.tone} /></View><DetailRow label="Due date" value={formatDate(vaccination.dueDate)} />{vaccination.completedAt ? <DetailRow label="Vaccination date" value={formatDate(vaccination.completedAt)} /> : null}</BaseCard></Screen>; }
